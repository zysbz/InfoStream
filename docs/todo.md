# TODO Backlog

- Transcription retry policy, quotas, and backoff
- FastAPI query/management interface
- Text diff for version visualization
- Near-duplicate and semantic deduplication
- Per-site rate-limit handling and adaptive caching
- Bilibili UP page ingestion plugin implementation
- C++ crawler runtime integration over NDJSON stream/file